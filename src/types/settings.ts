export interface AppSettings {
  theme: string;
  queryLimit: string;
  ollamaEndpoint: string;
  ollamaModel: string;
  editorFontSize: string;
  editorTabSize: string;
  graphLayout: string;
  graphShowColumns: string;
  graphShowTypes: string;
}
